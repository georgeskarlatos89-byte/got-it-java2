package com.cardinalcommerce.a;

public abstract class ECKey {

    public static class Cardinal {
        /* access modifiers changed from: private */
        public static final Cardinal Cardinal = new Cardinal();

        private Cardinal() {
        }
    }

    public static void Cardinal(byte[] bArr, int i, byte[] bArr2, int i2) {
        int[] configure = DeflateHelper.configure();
        int[] configure2 = DeflateHelper.configure();
        Curve.Cardinal(Cardinal.Cardinal, bArr, 0, configure, configure2);
        DeflateHelper.init(configure2, configure, configure, configure2);
        DeflateHelper.cca_continue(configure2, configure2);
        DeflateHelper.getInstance(configure, configure2, configure);
        DeflateHelper.configure(configure);
        DeflateHelper.init(configure, bArr2, 0);
    }
}
