package com.cardinalcommerce.a;

import com.cardinalcommerce.a.GM;
import java.io.ByteArrayOutputStream;
import java.io.IOException;

public final class DH extends getEnvironment {
    public DH(getBackgroundColor getbackgroundcolor) {
        super(getbackgroundcolor);
    }

    /* access modifiers changed from: package-private */
    public final int Cardinal() throws IOException {
        return getEncoded().length;
    }

    public final class Mappings extends getThreeDSRequestorAppURL implements isLocationDataConsentGiven {
        private final byte[] Cardinal;

        /* access modifiers changed from: package-private */
        public final int Cardinal() {
            return GM.Mappings.Cardinal(this.Cardinal.length) + 1 + this.Cardinal.length;
        }

        public final String cca_continue() {
            return getTextFontSize.getInstance(this.Cardinal);
        }

        /* access modifiers changed from: package-private */
        public final boolean configure() {
            return false;
        }

        /* access modifiers changed from: package-private */
        public final boolean configure(getThreeDSRequestorAppURL getthreedsrequestorappurl) {
            if (!(getthreedsrequestorappurl instanceof Mappings)) {
                return false;
            }
            return getBackgroundColor.Cardinal(this.Cardinal, ((Mappings) getthreedsrequestorappurl).Cardinal);
        }

        public final int hashCode() {
            return getBackgroundColor.init(this.Cardinal);
        }

        public Mappings(byte[] bArr) {
            byte[] bArr2;
            if (bArr == null) {
                bArr2 = null;
            } else {
                byte[] bArr3 = new byte[bArr.length];
                System.arraycopy(bArr, 0, bArr3, 0, bArr.length);
                bArr2 = bArr3;
            }
            this.Cardinal = bArr2;
        }

        /* access modifiers changed from: package-private */
        public final void getInstance(getRenderType getrendertype) throws IOException {
            byte[] bArr = this.Cardinal;
            getrendertype.getInstance(21);
            getrendertype.configure(bArr.length);
            getrendertype.configure.write(bArr);
        }
    }

    /* access modifiers changed from: package-private */
    public final void getInstance(getRenderType getrendertype) throws IOException {
        ByteArrayOutputStream byteArrayOutputStream = new ByteArrayOutputStream();
        if (this.Cardinal != null) {
            byteArrayOutputStream.write(this.Cardinal.configure("DL"));
        }
        if (this.getInstance != null) {
            byteArrayOutputStream.write(this.getInstance.configure("DL"));
        }
        if (this.cca_continue != null) {
            byteArrayOutputStream.write(this.cca_continue.configure("DL"));
        }
        byteArrayOutputStream.write(new getPayment(true, this.init, this.configure).configure("DL"));
        byte[] byteArray = byteArrayOutputStream.toByteArray();
        getrendertype.Cardinal(32, 8);
        getrendertype.configure(byteArray.length);
        getrendertype.configure.write(byteArray);
    }
}
