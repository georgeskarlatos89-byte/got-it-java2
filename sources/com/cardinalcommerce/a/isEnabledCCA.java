package com.cardinalcommerce.a;

import com.cardinalcommerce.a.DSTU4145;

public final class isEnabledCCA {
    private static isEnableLogging Cardinal = new getProcessorTransactionId();
    private static setEnableDFSync cca_continue = new getType();

    isEnabledCCA() {
    }

    public static isEnableLogging getInstance(getBackgroundColor getbackgroundcolor) {
        return getbackgroundcolor.getInstance.size() <= 0 ? Cardinal : new DSTU4145.Mappings(getbackgroundcolor);
    }

    static setEnableDFSync init(getBackgroundColor getbackgroundcolor) {
        return getbackgroundcolor.getInstance.size() <= 0 ? cca_continue : new DSTU4145(getbackgroundcolor);
    }
}
