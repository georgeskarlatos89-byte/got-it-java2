package com.cardinalcommerce.a;

import com.cardinalcommerce.a.GM;
import java.io.IOException;

public final class isValidated extends getThreeDSRequestorAppURL implements isLocationDataConsentGiven {
    private final byte[] init;

    isValidated(byte[] bArr) {
        this.init = bArr;
    }

    /* access modifiers changed from: package-private */
    public final int Cardinal() throws IOException {
        return GM.Mappings.Cardinal(this.init.length) + 1 + this.init.length;
    }

    public final String cca_continue() {
        return getTextFontSize.Cardinal(this.init);
    }

    /* access modifiers changed from: package-private */
    public final boolean configure() {
        return false;
    }

    /* access modifiers changed from: package-private */
    public final boolean configure(getThreeDSRequestorAppURL getthreedsrequestorappurl) {
        if (!(getthreedsrequestorappurl instanceof isValidated)) {
            return false;
        }
        return getBackgroundColor.Cardinal(this.init, ((isValidated) getthreedsrequestorappurl).init);
    }

    public final int hashCode() {
        return getBackgroundColor.init(this.init);
    }

    public final String toString() {
        return cca_continue();
    }

    /* access modifiers changed from: package-private */
    public final void getInstance(getRenderType getrendertype) throws IOException {
        byte[] bArr = this.init;
        getrendertype.getInstance(12);
        getrendertype.configure(bArr.length);
        getrendertype.configure.write(bArr);
    }
}
