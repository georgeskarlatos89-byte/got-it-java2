package com.cardinalcommerce.a;

import com.cardinalcommerce.dependencies.internal.minidev.json.JSONArray;
import com.cardinalcommerce.dependencies.internal.minidev.json.JSONObject;
import com.cardinalcommerce.dependencies.internal.nimbusds.jose.util.Base64;
import java.text.ParseException;
import java.util.List;

public final class setKeepScreenOn {
    setKeepScreenOn() {
    }

    public static setNextClusterForwardId configure(JSONObject jSONObject) throws ParseException {
        try {
            return setNextClusterForwardId.Cardinal((String) setAccessibilityPaneTitle.init(jSONObject, "kty", String.class));
        } catch (IllegalArgumentException e) {
            throw new ParseException(e.getMessage(), 0);
        }
    }

    public static List<Base64> getInstance(JSONObject jSONObject) throws ParseException {
        List<Base64> configure = setAccessibilityPaneTitle.configure((JSONArray) setAccessibilityPaneTitle.init(jSONObject, "x5c", JSONArray.class));
        if (configure == null || !configure.isEmpty()) {
            return configure;
        }
        return null;
    }
}
