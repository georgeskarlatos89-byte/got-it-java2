package com.cardinalcommerce.a;

import java.io.IOException;

public final class isEnabledVisaCheckout extends setUiType {
    public static final isEnabledVisaCheckout configure = new isEnabledVisaCheckout();
    private static final byte[] getInstance = new byte[0];

    /* access modifiers changed from: package-private */
    public final int Cardinal() {
        return 2;
    }

    /* access modifiers changed from: package-private */
    public final boolean configure() {
        return false;
    }

    /* access modifiers changed from: package-private */
    public final void getInstance(getRenderType getrendertype) throws IOException {
        byte[] bArr = getInstance;
        getrendertype.getInstance(5);
        getrendertype.configure(bArr.length);
        getrendertype.configure.write(bArr);
    }
}
