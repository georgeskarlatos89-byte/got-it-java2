package com.cardinalcommerce.a;

public final class getXid {
    private static getPaResStatus cca_continue = new getPaResStatus();

    static {
        new setEnabledDiscover();
    }

    getXid() {
    }

    public static getPaResStatus init(getBackgroundColor getbackgroundcolor) {
        return getbackgroundcolor.getInstance.size() <= 0 ? cca_continue : new getPaResStatus(getbackgroundcolor);
    }
}
