package com.cardinalcommerce.a;

import com.cardinalcommerce.a.GM;
import java.io.IOException;

public final class SubjectPublicKeyInfo extends getThreeDSRequestorAppURL implements isLocationDataConsentGiven {
    private final byte[] configure;

    SubjectPublicKeyInfo(byte[] bArr) {
        this.configure = bArr;
    }

    /* access modifiers changed from: package-private */
    public final int Cardinal() {
        return GM.Mappings.Cardinal(this.configure.length) + 1 + this.configure.length;
    }

    public final String cca_continue() {
        return getTextFontSize.getInstance(this.configure);
    }

    /* access modifiers changed from: package-private */
    public final boolean configure() {
        return false;
    }

    /* access modifiers changed from: package-private */
    public final boolean configure(getThreeDSRequestorAppURL getthreedsrequestorappurl) {
        if (!(getthreedsrequestorappurl instanceof SubjectPublicKeyInfo)) {
            return false;
        }
        return getBackgroundColor.Cardinal(this.configure, ((SubjectPublicKeyInfo) getthreedsrequestorappurl).configure);
    }

    public final int hashCode() {
        return getBackgroundColor.init(this.configure);
    }

    public final String toString() {
        return cca_continue();
    }

    /* access modifiers changed from: package-private */
    public final void getInstance(getRenderType getrendertype) throws IOException {
        byte[] bArr = this.configure;
        getrendertype.getInstance(26);
        getrendertype.configure(bArr.length);
        getrendertype.configure.write(bArr);
    }
}
